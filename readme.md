<h1 align="center">🚆 Trainscape Travel — Parallax Website</h1>
<p align="center">Анимированный сайт с параллакс-эффектами, адаптивной версткой и минималистичным интерфейсом.</p>

---

## 🌄 О проекте

Trainscape Travel — это экспериментальный веб-проект с анимацией скролла, динамическим параллакс-движением и адаптивной сеткой.  
Сайт демонстрирует работу многослойных изображений, эффекты движения по вертикали и горизонтали, а также базовые интерактивные карточки маршрутов.

Структура сайта включает:
- анимированную шапку (header) с поездами;
- блок маршрутов с hover-эффектами;
- вторую параллакс-сцену с кораблём и камнями;
- контактную форму;
- футер с социальными ссылками.

---

## 📱 Адаптивность

Сайт полностью адаптирован под:
- большие мониторы (1200px+),
- ноутбуки,
- планшеты,
- телефоны,
- маленькие устройства (до 380px).

Использованы современные media-queries, которые корректируют:
- размеры текста,
- сетки,
- высоты контейнеров,
- размещение контента,
- навигацию на мобильных устройствах.

---

## 🎨 Используемые технологии

- **HTML5**
- **CSS3 (Grid, Flexbox, адаптивные media-queries)**
- **Vanilla JavaScript (Parallax Scroll Animation)**
- **Custom Properties (CSS Variables)**
- **Без библиотек и фреймворков**

---

## ⚙️ JavaScript анимация

Параллакс реализован вручную с помощью события `scroll`:

```js
document.addEventListener("scroll", () => {
    let value = window.scrollY;

    imgTwo.style.left = value + "px";
    imgThree.style.bottom = -0.4 * value + "px";
    imgFour.style.bottom = -0.8 * value + "px";
    info.style.bottom = 350 + value + "px";

    ship.style.left = -window.innerHeight + 390 + 0.4 * value + "px";
    mountains.style.bottom = -window.innerHeight + 740 + 0.2 * value + "px";
});
